# @package _global_
defaults:
  - override /model: transocr
  - override /charset: 36_lowercase
  - override /dataset: Union
  
model:
  patch_size: [ 4, 4 ]
  parseq_pretrained: False
  parseq_checkpoint: /home/gaoz/output/flip/flipae_vit_base_patch16_dec256d4b-20e/checkpoint-10.pth
  encoder_pretrained: False
  encoder_checkpoint: $pretrained_path$
  batch_size: 192
  dec_depth: 6
  lr: 0.0003


data:
  root_dir: $finetune_data_path$
  num_workers: 8
  augment: False

trainer:
  # val_check_interval: 5
  max_epochs: 10
  accelerator: gpu
  devices: 2


exp_name: trstr-dec6-union-384-10e


hydra:
  output_subdir: config
  run:
    dir: /home/gaoz/outputs/${model.name}/${exp_name}/${now:%Y-%m-%d}_${now:%H-%M-%S}
  sweep:
    dir: /home/gaoz/multirun/${model.name}/${now:%Y-%m-%d}_${now:%H-%M-%S}
    subdir: ${hydra.job.override_dirname}

