# @package _global_
defaults:
  - override /model: transocr
  - override /charset: 94_full
  - override /dataset: Union
  
model:
  enc_embed_dim: 768
  enc_num_heads: 12
  enc_mlp_ratio: 4
  enc_depth: 12
  dec_embed_dim: 768
  dec_num_heads: 8
  dec_mlp_ratio: 4
  dec_depth: 4
  patch_size: [ 4, 4 ]
  parseq_pretrained: False
  parseq_checkpoint: /home/gaoz/output/flip/flipae_vit_base_patch16_dec256d4b-20e/checkpoint-10.pth
  encoder_pretrained: True
  encoder_checkpoint: /home/sist/zuangao/output/hybrid-mul-base-realrec/checkpoint-19.pth
  batch_size: 192

data:
  root_dir: /home/sist/zuangao/datasets/unidata/data/
  num_workers: 8
  augment: False

trainer:
  # val_check_interval: 5
  accelerator: gpu
  devices: 4


exp_name: transocr-vitbase-4A100-192x4-Union-4x4-noaug-94full-prerealrec


hydra:
  output_subdir: config
  run:
    dir: /home/sist/zuangao/outputs/${model.name}/${exp_name}/${now:%Y-%m-%d}_${now:%H-%M-%S}
  sweep:
    dir: /home/sist/zuangao/multirun/${model.name}/${now:%Y-%m-%d}_${now:%H-%M-%S}
    subdir: ${hydra.job.override_dirname}

